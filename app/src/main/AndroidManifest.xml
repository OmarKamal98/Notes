<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omar98K.notes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".activities.AddNewNote"/>
        <activity android:name=".activities.ChooseNoteBookStyle" />
        <activity android:name=".activities.EditeNote" />
        <activity android:name=".activities.ShowNote" />
        <activity android:name=".activities.Create_Notebook_Page" />
        <activity android:name=".activities.NoteBooksShowAll" />
        <activity android:name=".activities.NotesShowAll" />
        <activity android:name=".activities.Home_Page" />
        <activity android:name=".activities.Confirmation_message" />
        <activity android:name=".activities.Forgot_password" />
        <activity android:name=".activities.Sign_in" />
        <activity android:name=".activities.Sign_up" />
        <activity android:name=".activities.Splash" />
        <activity android:name=".activities.Note_Tutorial2" />
        <activity android:name=".activities.Notebook_Tutorial_1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />


</manifest>